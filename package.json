{
  "name": "metalsmith-micro-starter",
  "version": "0.0.1",
  "description": "A component-based micro starter for Metalsmith to be used with Claude",
  "type": "module",
  "keywords": [
    "metalsmith",
    "starter",
    "components",
    "structured-content",
    "static-site-generator"
  ],
  "author": "werner@glinka.co",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/wernerglinka/metalsmith-micro-starter.git"
  },
  "scripts": {
    "dev": "metalsmith -c metalsmith.js --env NODE_ENV=development",
    "dev:debug": "metalsmith -c metalsmith.js --env NODE_ENV=development --env DEBUG=@metalsmith*",
    "dev:subdomain": "metalsmith -c metalsmith.js --env NODE_ENV=development --env BASE_PATH=metalsmith-micro-starter",
    "dev:subdomain:debug": "metalsmith -c metalsmith.js --env NODE_ENV=development --env DEBUG=@metalsmith* --env BASE_PATH=metalsmith-micro-starter",
    "build": "metalsmith -c metalsmith.js --env NODE_ENV=production",
    "build:subdomain": "metalsmith -c metalsmith.js --env NODE_ENV=production --env BASE_PATH=metalsmith2025-simple-starter",
    "start": "NODE_ENV=development node metalsmith.js --watch",
    "start:debug": "NODE_ENV=development DEBUG=@metalsmith* node metalsmith.js --watch",
    "start:subdomain": "NODE_ENV=development BASE_PATH=metalsmith2025-simple-starter node metalsmith.js --watch",
    "start:subdomain:debug": "NODE_ENV=development DEBUG=@metalsmith* BASE_PATH=metalsmith2025-simple-starter node metalsmith.js --watch",
    "serve": "browser-sync start --server 'build'",
    "format": "prettier --write \"**/*.{js,json,njk,css}\"",
    "lint": "eslint --fix .",
    "fix": "npm run format && npm run lint",
    "depcheck": "depcheck",
    "release": "./scripts/release.sh",
    "release:patch": "./scripts/release.sh patch",
    "release:minor": "./scripts/release.sh minor",
    "release:major": "./scripts/release.sh major"
  },
  "devDependencies": {
    "auto-changelog": "^2.5.0",
    "browser-sync": "^3.0.4",
    "eslint": "^9.33.0",
    "gray-matter": "^4.0.3",
    "mocha": "^11.7.1",
    "prettier": "^3.6.2",
    "prettier-plugin-jinja-template": "^2.1.0",
    "release-it": "^19.0.4"
  },
  "dependencies": {
    "@metalsmith/drafts": "^1.3.0",
    "@metalsmith/layouts": "^3.0.0",
    "@metalsmith/metadata": "^0.3.0",
    "@metalsmith/permalinks": "^3.2.0",
    "autoprefixer": "^10.4.21",
    "cssnano": "^7.1.0",
    "depcheck": "^1.4.7",
    "jstransformer-nunjucks": "^1.2.0",
    "marked": "^16.2.0",
    "metalsmith": "^2.6.3",
    "metalsmith-bundled-components": "^0.4.1",
    "metalsmith-menu-plus": "^0.2.0",
    "metalsmith-optimize-html": "^0.7.0",
    "metalsmith-optimize-images": "^0.10.2",
    "metalsmith-safe-links": "^2.1.0",
    "metalsmith-seo": "^0.0.1",
    "metalsmith-sitemap": "^1.2.2",
    "metalsmith-static-files": "^2.0.0",
    "postcss": "^8.5.6"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
